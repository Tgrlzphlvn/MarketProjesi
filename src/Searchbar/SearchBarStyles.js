import {StyleSheet} from 'react-native';

export const searchBarStyles = StyleSheet.create({
  input: {
    margin: 20,
    borderWidth: 1,
    height: 50,
    padding: 10,
    borderRadius: 10,
  },
});
