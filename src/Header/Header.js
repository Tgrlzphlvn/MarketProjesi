import {Text, View} from 'react-native';
import React from 'react';
import {headerStyles} from './HeaderStyles';

const Header = () => {
  return <Text style={headerStyles.title}>PATIKASTORE</Text>;
};

export default Header;
